assets/sprites/*.png filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.ogg filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text

# 260722 Red data/*.json 在仓库里实际全部是LF存储（已核对 core.autocrlf=true 会在commit时
# 自动转成LF），但工作区一旦被某个工具用纯LF写入（跳过CRLF），就会被git的autocrlf误判成
# "待转换"而显示为已修改——哪怕内容跟HEAD逐字节相同（git diff是空的，只有换行符警告）。
# 显式钉死这一批文件为LF，让工作区和仓库存储永远一致，不再有这种噪声
data/*.json text eol=lf
